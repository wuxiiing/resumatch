# ResuMatch Task Plan

> 本任务清单只覆盖交接文件中的第一版范围。任何新增能力都需要先在核心窗口确认。

## 阶段 0：项目地基

目标：确认项目目录、保存项目规则、准备后续开发。

- [x] 建立项目根目录 `D:\AI-workspace\Codex\ResuMatch`。
- [x] 添加项目范围文档 `PROJECT_SCOPE.md`。
- [x] 添加项目协作文档 `PROJECT_WORKFLOW.md`。
- [x] 添加阶段任务清单 `TASKS.md`。
- [x] 明确命名施工窗口和多窗口协作规则。
- [x] 复制交接文件到项目根目录，作为需求源。
- [x] 添加前端设计指南 `FRONTEND_DESIGN_GUIDE.md`，作为阶段 1 页面骨架和视觉风格依据。
- [x] 确认只使用 D 盘目录；C 盘空仓库不再作为开发目录。

## 阶段 1：项目骨架

目标：搭建 Next.js + Tailwind 基础项目，不实现复杂业务。

- [x] 初始化 Next.js App Router 项目。
- [x] 配置 Tailwind CSS。
- [x] 建立 `app`、`components`、`lib`、`types` 基础结构。
- [x] 创建基础首页和报告页占位。
- [x] 确认可本地启动。

验收标准：本地页面能打开，无业务功能扩展。

## 阶段 1.5：首页体验增强

目标：在前端首页和报告页 checkpoint 后，加入项目负责人明确批准的首页开场动画体验增强，不扩大业务功能范围。

- [ ] 整合 `E:\resumatch_open_v10.html` 的开场动画为 React / Next.js 可复用组件。
- [ ] 保留动画流程：两条竖线水平飞入、中心吸附/碰撞、水平飞出、`RM` 展开、`RESUMATCH` 展开、Logo 上移、主页面内容淡入。
- [ ] 去掉残影效果，不重新加入残影。
- [ ] 线条飞出只保持水平左右方向，不斜飞。
- [ ] 重播按钮在开发验收阶段可见，正式版可隐藏或放在不明显位置。
- [ ] 支持 `prefers-reduced-motion`，避免影响减少动态效果的用户。
- [ ] 不修改主页业务逻辑，不修改后端/API/解析/DeepSeek/导出/限流。

验收标准：动画自然、克制、可重播，不遮挡用户操作；`npm run lint` 和 `npm run build` 通过。

## 阶段 2：文件解析主链路

目标：先跑通最稳定的简历文本提取。

- [ ] 实现 `.docx` 解析。
- [ ] 实现简历文本预览。
- [ ] 实现 3000 字限制。
- [ ] 再实现 `.xlsx` 解析。
- [ ] 再实现文字型 `.pdf` 解析。
- [ ] 最后接百度 OCR 兜底。

验收标准：至少 `.docx` 可以稳定提取中文文本并显示预览。

## 阶段 3：JD 输入和提交校验

目标：完成 JD 输入体验和前端提交前校验。

- [ ] 创建 JD 输入框。
- [ ] 实现 1000 字计数。
- [ ] 超限变红并阻止提交。
- [ ] 简历和 JD 都满足条件时才能点击开始分析。

验收标准：前端不允许超范围文本进入分析接口。

## 阶段 4：DeepSeek 分析接口

目标：后端调用 DeepSeek 并返回合规 JSON。

- [ ] 创建 `/api/analyze`。
- [ ] 在后端读取 `DEEPSEEK_API_KEY`。
- [ ] 固定 system prompt。
- [ ] 拼装 user prompt。
- [ ] 调用 `deepseek-chat`。
- [ ] 校验 JSON。
- [ ] JSON 解析失败时重试一次。
- [ ] 仍失败时返回 500。

验收标准：接口能返回符合 `types/analysis.ts` 的结构化数据。

## 阶段 5：报告页渲染

目标：展示匹配度和简历标注报告。

- [ ] 创建匹配度仪表盘。
- [ ] 展示已匹配关键词。
- [ ] 展示缺失关键词。
- [ ] 展示优化建议数量。
- [ ] 按 `segments.original` 匹配简历原文。
- [ ] 根据 `status` 渲染绿色、黄色、灰色背景。
- [ ] 找不到原文片段时跳过，不报错。
- [ ] 黄色片段展示 comment 和 suggestion。

验收标准：报告只读，不提供网页内编辑能力。

## 阶段 6：IP 次数限制

目标：限制 API 成本和滥用。

- [ ] 实现每 IP 每天 5 次限制。
- [ ] 实现全站每天 500 次限制。
- [ ] 超 IP 限制返回 429。
- [ ] 超全站限制返回 503。
- [ ] 明确使用内存方案还是 Vercel KV。

验收标准：限制逻辑在 `/api/analyze` 前置生效。

## 阶段 7：Word 导出

目标：导出干净 Word 报告。

- [ ] 使用 docx.js 生成 Word。
- [ ] 第一部分：分析报告摘要。
- [ ] 第二部分：简历原文干净版。
- [ ] 第三部分：优化建议清单。
- [ ] 不在 Word 中加入颜色标注。

验收标准：Word 可打开，内容清楚，适合用户自行修改。

## 阶段 8：整体联调和部署

目标：完整跑通第一版流程。

- [ ] 上传简历。
- [ ] 粘贴 JD。
- [ ] 分析。
- [ ] 查看报告。
- [ ] 导出 Word。
- [ ] 配置 Vercel 环境变量。
- [ ] 部署到 Vercel。
- [ ] 用真实样例验收。

验收标准：主流程完整可用，未加入第一版以外功能。

## 当前待确认问题

- [x] 是否正式只使用 `D:\AI-workspace\Codex\ResuMatch` 作为项目目录。已确认。
- [x] C 盘 `C:\Users\Administrator\Documents\简历颗粒度对齐网站` 是否废弃、保留，还是迁移 Git。已确认不作为开发目录。
- [ ] 第一版限流使用内存方案还是 Vercel KV。
- [ ] 百度 OCR 是否第一阶段就接入，还是等 PDF 主流程跑通后再接。
- [ ] 是否已有 DeepSeek 和百度 OCR API Key。

