# HANDOFF 2026-07-04 · 竹林输入页 + 侦探化研判收口 → R2 模板系统(交接给 Opus)

> 接手必读顺序:本文件 → `V2-PRODUCT-PLAN.md`(master 路线图)→ `V2-INPUT-REVAMP-SPEC.md`(输入页细节)。
> 铁律:先诊断后改;**判级规则(③ signals→后端 decideVerdict)是命根子,不许动**;新国风 token(gf-*)不换族;"不装作知道"(推断必须带概率+证据+非确定标注)是产品原则。

## 一、这一程做完了什么(全部实测过,typecheck 0 错)

**P1 豆包视觉(眼睛)** `lib/agents/doubao-vision.ts` + `/api/extract-image`
- Ark chat/completions(不是 Responses API);`thinking:{type:"disabled"}` 是关键坑(不关 210s,关了 27.5s)。
- env:`ARK_API_KEY`,模型默认 `doubao-seed-1-6-vision-250815`。

**P2 槽位入场(前门)** `lib/agents/intake.ts`(文字分诊 jd/goal/resume/other)+ `vision-intake.ts`(图片分诊:简历→StructuredResume / JD截图→全文)+ `/api/intake-text`;纯逻辑状态机 `intake-steps.ts`(缺啥问啥,前后端共用)。

**P3 输入页(脸)** `app/agent/page.tsx` 整页重写
- 竹林运镜视频背景(`public/bamboo/intro.mp4` + `poster.png`;素材缺席自动降级渐变)。
- 玻璃对话框:标题(14vh)与面板(max(34vh,240px))同时浮现;GPT 式紧凑(只显军师最新一句);槽位小票带 ✕ 换料;上传前压图 ≤1280px;右上「上次研判 →」。
- 落叶=SVG 披针竹叶;背景鼠标视差;**面板不做 transform 视差**(Chrome backdrop-blur+每帧transform 会丢渲染层=之前"框消失"病根)。

**R1 结果页(决策优先)** `app/agent-result/page.tsx`
- 墨绿(gf-greend)头版 + 朱印裁决章;「为什么这么判」;orgGuess 推断卡(概率条+JD原词依据+非确定标注,实测:米哈游100%/字节·豆包85%,判级未被带偏)。
- ① 新增 `jobTitle`(≤14字提炼,自动命名+meta.position)+ `orgGuess`(schema+校验在 `lib/agents/jd-analysis/`);③ prompt 禁内部字段名;② 引文≤60字+claim人话。
- 节节高=真竹节:连续竹竿+节线,1/2/3 节固定尺寸(用户对"积木块"版发过火,别改回去)。
- 「逐条对照」(原名"排雷"已废);牌面 claim 优先+引文小字;全页 font-serifcn;字阶 28/18/15军师语/14/12/11。
- AppShell 加 `tone`:研判=宣纸(默认)/ 简历台=`#f7f1e3` / 小简=`#eef0e0`。
- AppNav:「我的简历/问小简」置顶;历史条 hover ✕ 删除(`removeFromHistory`)。

**字体**:tailwind `serifcn: ["SimSun","Songti SC","serif"]` —— 用户机上思源/STSong 疑似残缺子集导致"部分字借字粗细花",SimSun 置顶是止血;正解见下"改进意见 f"。**tailwind.config 改动必须重启 dev server 才生效**(用户被坑过一次)。

## 二、下一步 = R2 模板系统(已启动,考察完毕)

参考源码已浅克隆:`D:\AI-workspace\Temp\claude\D--AI-workspace-projects\6a71bfa6-...\scratchpad\reactive-resume`(Reactive Resume v5)。结论:
- 模板 = **@react-pdf/renderer React 组件**(packages/pdf/src/templates/*/XxxPage.tsx),`renderToBuffer` 直出 PDF —— 无浏览器页眉页脚问题;同组件可做浏览器实时预览 → editor|preview 双栏天然成立。
- schema 用 zod 且全字段 `.describe()`(AI 填充友好,StructuredResume 可借鉴)。

**R2-1 spike(先做,是一切的模子)**:装 `@react-pdf/renderer`;`Font.register` 本地中文字体(下载 Noto Sans SC + Noto Serif SC ttf 放 `public/fonts/`);用 `StructuredResume`(`lib/resume-structured.ts`)渲第一套 **ats-classic** 模板;`/api/export-pdf` 或客户端直接 `pdf().toBlob()` 下载。**验收 = 中文不乱码、断行正常、两页分页正确。CJK 断行是 react-pdf 已知弱项,spike 先验证排版质量再全押;不行的备选:打印 CSS 优化(@page + print:hidden,半成品已有)或服务端 puppeteer。**
**R2-2**:`/resume` 页(现 `ResumeWorkbench.tsx`)改 editor|preview 双栏(左填空式表单/对话式,右实时预览)。
**R2-3**:apple / notion 变体(照 ats 骨架复刻,可派低级模型)+ **ai-pro**(独家:第2页=研判摘要 Why Match/Best Fit/Gap,数据从 `lib/agent-report.ts` 的 AgentReport 取,历史在 localStorage `lib/history.ts`)。
**R2-4**:JSON Resume v1 ↔ StructuredResume 映射(纯函数+单测,可派)+ DOCX(项目已有 docx 依赖)。

**可直接派给便宜模型的任务**(规格闭合):字体文件下载摆放 / R2-4 映射函数 / apple+notion 复刻(等 ats 出模)/ inspirations 素材整理。

## 三、遗留小活(顺手清)
- 打印 CSS 短修(`/resume` window.print 带页眉页脚;若 R2-1 走 react-pdf 则此项作废)。
- impeccable skill 要求的 PRODUCT.md/DESIGN.md 档案未建(跑 `/impeccable init`;注意 context.mjs 要 `--target` 指到 ResuMatch)。
- 最终竹林视频只有 1080p/约5s,R3 可考虑 webm 压缩(首屏流量)。

## 四、架构改进意见(按性价比排序,做前先跟用户过)
a. **①② 并行化**(graph.ts:② 不依赖 ①,并行省 15-20s,研判 30-60s 太长是最大体感痛点)。
b. **intake 启发式短路**:文本含"职责/任职要求"且>300字 → 直接判 jd,不调 LLM(省 2-4s+费用;短文本再走模型)。
c. **字体正解**:R2 反正要为 react-pdf 引入 Noto 字体文件 —— 顺手用 `next/font/local` 给网页也换上,网页/PDF 同字体,替掉 SimSun 权宜(一鱼两吃)。
d. API route 样板重复(JSON parse/限流/错误三段式 ×6 个 route)→ 抽一个小 helper(纯清洁,不急)。
e. rate-limit 内存版多实例失效(已知),上生产前换 Vercel KV。
f. 历史 localStorage 无上限 → cap 50 条。
g. 单测缺位:`intake-steps`、R2-4 映射函数值得补(便宜、防回归)。
h. 视觉分诊可试 `doubao-1.5-vision-lite` 对照(1.5元/M,若精度够则默认走 lite,pro 兜底)。

## 五、怎么跑 / 验
- dev:`npm run dev`(ResuMatch 目录);typecheck:`node node_modules\typescript\bin\tsc --noEmit`。
- 管道脚本:`node --experimental-strip-types --env-file=.env.local scripts/test-intake.mts [图片路径]` / `scripts/test-vision.mts <图>`。
- 结果页无真数据时:sessionStorage 塞 `jianpei:last-report`(AgentReport 形状见 `lib/agent-report.ts`)。
- `.env.local`:DEEPSEEK_*(文本)/ TAVILY(背调)/ ARK_API_KEY(视觉)。**勿打印勿提交**(AGENTS.md 红线,本程曾误踩,已道歉)。

## 六、坑清单(血泪版)
1. 平台"安检分类器"(deepseek 后台)会宕:Bash/Skill/preview/Agent 全拦,报 `deepseek-v4-pro[1m] unavailable` —— 等一会重试,读写文件不受影响。Agent 派单(subagent)在此平台**当前模型路由是坏的**,派单请用户自己开新窗口。
2. tailwind.config 改动要**重启 dev server**;代码改动 HMR 即可。
3. `ch` 单位按半角算:中文排版别用 max-w-[Nch](上过当:68ch≈480px)。
4. Chrome:backdrop-blur 元素别每帧改 transform(会间歇丢层)。
5. 用户桌面 `E:\桌面` 有身份证/户口等敏感件,**只碰明确给的文件**;简历图在 `E:\桌面\简历.jpg`。
6. Blender 工程 `E:\桌面\resumatch.blend`;帧序列在 scratchpad `final_seq3/`(1080p 定稿);ffmpeg 在 `_envs/video-automation-agent/tools/hyperframes-ffmpeg/...`。
7. 用户风格:ADHD——一次一个动作、少选项;审美极简克制(Moonshot 向),"一点红"原则;红绿相撞、薄荷绿、积木感都被否过;改前先看他原话,改完给实测证据(他会揪"你真改了吗")。
